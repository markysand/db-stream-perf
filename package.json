{
  "name": "app",
  "version": "1.0.0",
  "description": "performance check streams vs full loads",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "populate": "node src/populate.js",
    "server": "node src/server.js"
  },
  "author": "Markus Sandén",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "mongo": "^0.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
